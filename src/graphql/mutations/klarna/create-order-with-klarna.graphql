mutation CreateOrderWithKlarna(
  $input: InitKlarnaSessionInput!
  $authorizationToken: String!
) {
  createOrderWithKlarna(
    input: $input
    authorizationToken: $authorizationToken
  ) {
    order_id
    redirect_url
    fraud_status
    authorized_payment_method {
      type
    }
  }
}
